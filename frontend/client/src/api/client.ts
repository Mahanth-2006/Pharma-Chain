const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || "http://localhost:8000/api";
export type Role = "manufacturer" | "distributor" | "pharmacy";
export type ApiOptions = RequestInit & { token?: string };
async function request<T>(path: string, options: ApiOptions = {}): Promise<T> { const { token, headers, ...init } = options; const response = await fetch(`${API_BASE_URL}${path}`, { ...init, headers: { "Content-Type": "application/json", ...(token ? { Authorization: `Bearer ${token}` } : {}), ...headers } }); if (!response.ok) throw new Error((await response.json().catch(() => null))?.message || `Request failed (${response.status})`); return response.json(); }
export const api = { auth: { login: (email: string, password: string, requestedRole: Role) => request<{ token: string; user: { name: string; email: string; role: Role | "hospital" } }>("/auth/login", { method: "POST", body: JSON.stringify({ email, password, role: requestedRole }) }) }, batches: { mint: (payload: Record<string, unknown>, token?: string) => request("/batches/mint", { method: "POST", body: JSON.stringify(payload), token }), addDistribution: (payload: Record<string, unknown>, token?: string) => request("/batches/distribution", { method: "POST", body: JSON.stringify(payload), token }), addPurchase: (payload: Record<string, unknown>, token?: string) => request("/batches/purchase", { method: "POST", body: JSON.stringify(payload), token }), history: (batchId: string, token?: string) => request(`/batches/${encodeURIComponent(batchId)}/history`, { token }) }, medicines: { search: (query: string, page = 1, limit = 8, token?: string) => request(`/medicines/search?q=${encodeURIComponent(query)}&page=${page}&limit=${limit}`, { token }) } };
export { API_BASE_URL };
