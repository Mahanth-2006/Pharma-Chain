import { Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
const rows = Array.from({ length: 8 }, (_, i) => ({ id: `MED-${(82410 + i * 137).toString()}`, name: ["Ceftriaxone 1g / vial", "Insulin glargine 100IU", "Atorvastatin 20mg", "Amoxicillin 500mg"][i % 4], maker: ["Helix Biologics", "NovaCare Labs", "Meridian Pharma"][i % 3], country: ["India", "Germany", "Singapore"][i % 3], status: "verified" }));
export default function SearchTable() { const [query, setQuery] = useState(""); const [page, setPage] = useState(1); const filtered = useMemo(() => rows.filter(r => Object.values(r).join(" ").toLowerCase().includes(query.toLowerCase())), [query]); return <div className="glass panel table-wrap"><div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginBottom: 14 }}><div className="input-wrap" style={{ flex: 1, maxWidth: 460, position: "relative" }}><Search size={15} color="#91a6a4" style={{ position: "absolute", left: 13, top: 14 }} /><input className="input" style={{ paddingLeft: 38 }} value={query} onChange={e => { setQuery(e.target.value); setPage(1); }} placeholder="Search 250,000 medicines, batches, manufacturers..." /></div><button className="icon-btn" aria-label="Filter dataset"><SlidersHorizontal size={16} /></button></div><div className="table-head"><span>Medicine / batch</span><span>Manufacturer</span><span>Origin</span><span>Status</span><span>Open</span></div>{filtered.map(row => <div className="table-row" key={row.id}><span><strong>{row.name}</strong><br /><span className="mono" style={{ color: "#7ef3cd", fontSize: 9 }}>{row.id}</span></span><span>{row.maker}</span><span>{row.country}</span><span><span className="badge">{row.status}</span></span><span><button className="icon-btn" aria-label={`Open ${row.id}`}>↗</button></span></div>)}<div className="pagination"><span>Showing {(page - 1) * 8 + 1}–{(page - 1) * 8 + filtered.length} of 250,000 records</span><div className="pagination-actions"><button className="icon-btn" onClick={() => setPage(Math.max(1, page - 1))}>←</button><button className="icon-btn" onClick={() => setPage(page + 1)}>→</button></div></div></div>; }
