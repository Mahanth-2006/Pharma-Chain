import type { CSSProperties } from "react";
export type LoaderVariant = "circular" | "classic" | "pulse" | "pulse-dot" | "dots" | "typing" | "wave" | "bars" | "terminal" | "text-blink" | "text-shimmer" | "loading-dots";
export function Loader({ variant = "dots", label = "", className = "" }: { variant?: LoaderVariant; label?: string; className?: string }) { return <span className={`loader loader-${variant} ${className}`} aria-label={label || "Loading"} role="status">{variant === "circular" && <span className="loader-circle" />}{variant === "classic" && <span>Loading</span>}{variant === "pulse" && <span className="loader-pulse" />}{variant === "pulse-dot" && <><span className="loader-pulse-dot" /><span className="loader-pulse-dot" /></>}{variant === "dots" && <><i /><i /><i /></>}{variant === "typing" && <><i /><i /><i /></>}{variant === "wave" && <><i /><i /><i /><i /></>}{variant === "bars" && <><i /><i /><i /><i /></>}{variant === "terminal" && <span>_</span>}{variant === "text-blink" && <span>SYNCING</span>}{variant === "text-shimmer" && <span>Loading</span>}{variant === "loading-dots" && <span>Loading...</span>}</span>; }
export const loaderStyle: CSSProperties = { display: "inline-flex", alignItems: "center", justifyContent: "center" };
