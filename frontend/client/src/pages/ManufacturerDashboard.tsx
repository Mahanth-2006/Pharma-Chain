import { ArrowRight, Plus } from "lucide-react";
import { useState } from "react";
import { api } from "../api/client";
import BatchCard from "../components/BatchCard";
import LiquidGlassPanel from "../components/LiquidGlassPanel";
import SearchTable from "../components/SearchTable";
import DashboardLayout from "./DashboardLayout";
export default function ManufacturerDashboard() { const [open, setOpen] = useState(false); const [saved, setSaved] = useState(false); const submit = async (e: React.FormEvent) => { e.preventDefault(); await api.batches.mint({ product: "Ceftriaxone 1g / vial" }); setSaved(true); setOpen(false); }; return <DashboardLayout eyebrow="manufacturer console" title={<>Mint with <em>confidence.</em></>} description="Create tamper-evident batch records and hand them into the live chain. Your plant is currently synced to 4 active lines." action={<button className="btn btn-primary" onClick={() => setOpen(true)}><Plus size={15} /> Mint new batch</button>}><div className="grid grid-2 fade-up delay-1"><LiquidGlassPanel hover><div className="stat-label">Batches minted</div><div className="stat-value">12,482</div><div className="stat-meta good">↑ 8.4% this month</div></LiquidGlassPanel><LiquidGlassPanel hover><div className="stat-label">In active chain</div><div className="stat-value">9,218</div><div className="stat-meta">74% of total records</div></LiquidGlassPanel></div>{saved && <div className="badge" style={{ margin: "18px 0" }}>Batch mint request queued · demo mode</div>}<div className="grid grid-2" style={{ marginTop: 16 }}><div><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}><h2 style={{ margin: 0, fontSize: 16 }}>Recent chain activity</h2><span className="mono" style={{ color: "#91a6a4", fontSize: 10 }}>LIVE / 24H</span></div><BatchCard /><div style={{ marginTop: 16 }}><BatchCard id="PC-2024-08-0187" product="Insulin glargine 100IU" /></div></div><LiquidGlassPanel className="fade-up delay-2"><div className="eyebrow">chain status</div><h2 style={{ margin: "14px 0 5px", fontSize: 20 }}>Production is moving</h2><p style={{ color: "#91a6a4", fontSize: 11, lineHeight: 1.6 }}>Your active lines are synced and the latest batch records are ready for downstream custody.</p><div className="status-rail"><span className="status-rail-dot" /><span>4 active lines</span><span className="status-rail-line" /><span>9,218 active records</span></div><div className="mini-bars">{[35,48,42,68,60,79,64,88,75,94,84,100].map((h, i) => <div key={i} style={{ height: `${h}%`, opacity: i > 8 ? 1 : .45 }} />)}</div></LiquidGlassPanel></div><div style={{ marginTop: 16 }}><div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}><h2 style={{ margin: 0, fontSize: 16 }}>Medicine index</h2><span className="mono" style={{ color: "#91a6a4", fontSize: 10 }}>250K RECORDS</span></div><SearchTable /></div>{open && <div style={{ position: "fixed", inset: 0, zIndex: 50, display: "grid", placeItems: "center", padding: 20, background: "rgba(0,5,6,.72)" }}><LiquidGlassPanel className="fade-up" style={{ maxWidth: 580, width: "100%" } as any}><div style={{ display: "flex", justifyContent: "space-between" }}><div><div className="eyebrow">new chain record</div><h2 style={{ margin: "12px 0 18px", fontSize: 22 }}>Mint medicine batch</h2></div><button className="icon-btn" onClick={() => setOpen(false)}>×</button></div><form className="form-grid" onSubmit={submit}><div className="input-wrap"><label>Product name</label><input className="input" defaultValue="Ceftriaxone 1g / vial" /></div><div className="input-wrap"><label>Batch number</label><input className="input" defaultValue="PC-2024-08-0195" /></div><div className="input-wrap"><label>Unit quantity</label><input className="input" type="number" defaultValue="24000" /></div><div className="input-wrap"><label>Expiry date</label><input className="input" type="date" defaultValue="2026-08-18" /></div><div className="input-wrap full"><label>Manufacturing notes</label><textarea className="input textarea" placeholder="Optional notes sealed into the record" /></div><div className="form-actions full"><button type="button" className="btn btn-ghost" onClick={() => setOpen(false)}>Cancel</button><button className="btn btn-primary">Commit to chain <ArrowRight size={14} /></button></div></form></LiquidGlassPanel></div>}</DashboardLayout>; }
